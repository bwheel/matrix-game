#pragma once

// TODO: move into a component of a singleton entity
namespace Globals {
// Define screen dimensions
const int SCREEN_WIDTH = 800;
const int SCREEN_HEIGHT = 600;
const int FONT_SIZE = 24;
const int NUM_COLUMNS = 1; // SCREEN_WIDTH / FONT_SIZE;
} // namespace Globals